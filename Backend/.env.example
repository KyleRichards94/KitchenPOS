# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL="file:./dev.db"

# Required for JWT signing (use a long random string in production)
JWT_SECRET="replace_me"

# Optional: access token lifetime (default 7d)
# JWT_EXPIRES_IN="7d"

# Optional: Svelte dev server origin for CORS
# CORS_ORIGIN="http://localhost:5173"


# For better examples of JWT auth search for, Express JWT auth Templates on github, they get very complrehensive. 

#this JWT auth only serves as a middleware between route entry points that require authentication. 
#Easily bypassed by stealing an auth token from somwhere. 